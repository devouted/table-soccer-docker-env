{% extends 'index/index.html.twig' %}

{% block title %}{{ 'tournament.new.title' | trans }}{% endblock %}

{% block content %}
    <h4 class="card-title mt-3 text-center">{{ 'tournament.new.title' | trans }}</h4>
    <hr>
    {{ include('tournament/_form.html.twig') }}

    <a class="btn btn-dark" href="{{ path('tournament_index') }}">{{ 'app.back_to_list' | trans }}</a>
{% endblock %}
